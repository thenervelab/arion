# Miner CLI configuration (example)
#
# This binary lets a miner (family account) register/deregister/claim against `pallet-arion`
# directly, using:
# - family mnemonic: signs the Substrate extrinsic (origin == family)
# - miner node key: signs the ed25519 "ARION_NODE_REG_V1" payload (node_sig)

# Chain websocket endpoint
CHAIN_WS_URL=<wss://your-node>

# Optional: pallet name if auto-detect fails (otherwise leave empty)
ARION_PALLET_NAME=

# Be sure to clean this file
# Family account mnemonic (sr25519)
FAMILY_MNEMONIC="YOUR COLD KEY MNEMONIC"

# Child account SS58 (proxy/delegate)
CHILD_SS58="YOUR HOT KEY SS58"

# Miner node data dir containing keypair.bin (ed25519)
MINER_DATA_DIR=data/miner





